<div class="item lottery-result <?=$lotteryClass?>">
    <div class="top-block">
        <div class="title">
            <b><?=$lotteryName?></b>
        </div>
    </div>
    <div class="center-block">
        <?php foreach($lotteryResults as $result) {
            if ($lotteryName === "MegaSena"){
                $result["regular_numbers"][] = $result["lucky_numbers"][1];
            }
            else if ($lotteryName === "SuperEnalotto"){
                $result["regular_numbers"][] = $result["lucky_numbers"][1];
                unset( $result["lucky_numbers"][1]);
            }
        ?>
            <div class="result-row" style="cursor: pointer;" onclick="window.location='<?= $link ?>/<?= $result["draw_date"]->format('Y-m-d')?>'">
                <div class="row--date">
                   <?=
                        $day[$result["draw_date"]->format('l')].' '. $result["draw_date"]->format('d-m-Y');
                   ?>
                </div>
                <div class="row--results">
                    <?php foreach($result["regular_numbers"] as $regular_number) {?>
                        <div class="ball">
                            <?= $regular_number ?>
                        </div>
                    <?php } ?>
                    <?php if(in_array($lotteryName, ['MegaMillions', 'PowerBall'])){ ?>
                        <div class="ball star">
                            <?= $result["lucky_numbers"][1] ?>
                        </div>
                        <div class="ball star">
                            <?= $result['raffle']?>X
                        </div>
                    <?php }else if (!in_array($lotteryName, ['MegaSena'])){ ?>
                    <?php foreach($result["lucky_numbers"] as $lucky_number) {?>
                        <div class="ball star">
                            <?= $lucky_number ?>
                        </div>
                    <?php } }?>
                </div>
            </div>
        <?php } ?>
    </div>
    <div class="bottom-block">
        <a href="/<?= $link ?>" class="bottom-block-link"><?= $checkMoreResults ?></a>
    </div>
</div>