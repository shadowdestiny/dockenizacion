<div class="item lottery-result <?=$lotteryClass?>">
    <div class="top-block">
        <div class="title">
            <b><?=$lotteryName?></b>
        </div>
    </div>
    <div class="center-block">
        <?php foreach($lotteryResults as $result) {?>
            <div class="result-row" style="cursor: pointer;" onclick="window.location='<?= mb_strtolower($lotteryName) ?>/results/draw-history/<?= $result["draw_date"]->format('Y-m-d')?>'">
                <div class="row--date">
                   <?=
                        $day[$result["draw_date"]->format('l')].' '. $result["draw_date"]->format('d-m-Y');
                   ?>
                </div>
                <div class="row--results">
                    <?php foreach($result["regular_numbers"] as $regular_number) {?>
                        <div class="ball">
                            <?= $regular_number ?>
                        </div>
                    <?php } ?>
                    <?php if(in_array($lotteryName, ['MegaMillions', 'PowerBall'])){ ?>
                        <div class="ball star">
                            <?= $result["lucky_numbers"][1] ?>
                        </div>
                        <div class="ball star">
                            <?= $result['raffle']?>X
                        </div>
                    <?php }else{ ?>
                    <?php foreach($result["lucky_numbers"] as $lucky_number) {?>
                        <div class="ball star">
                            <?= $lucky_number ?>
                        </div>
                    <?php } }?>
                </div>
            </div>
        <?php } ?>
    </div>
    <div class="bottom-block">
        <a href="/<?= mb_strtolower($lotteryName) ?>/results/draw-history" class="bottom-block-link"><?= $checkMoreResults ?></a>
    </div>
</div>