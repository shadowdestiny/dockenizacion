<div class="page-header">
	<h1 id="overview">Draw History</h1>
</div>

<div class="">
	<? if ($this->canPublish) { ?>
	<a class="popup btn btn-primary" data-href="<?=$this->url(Array('action' => 'activatedraw','language' => '0','lastdrawid' => $this->list[0]['draw_id']))?>">Publish Current Draw</a> This button publishes the current draw and creates a new empty draw in the database
		<br /><br />
	<? } else { ?>
	<div class="alert alert-info" role="alert"><strong>Notice:</strong> Please complete the current draw with result / prize data!<br />
	After this, you can publish the draw and create the next draw with Jackpot.</div>

	<? } ?>

</div>
<table class="table table-striped table-bordered">
	<tr>
		<th>Draw ID</th>
		<th>winner</th>
		<th><?=$this->translate('published')?></th>
		<th><?=$this->translate('date')?></th>
		<th>Jackpot</th>
		<th>Option</th>
	</tr>
	<? foreach($this->list as $item) { ?>
	<tr>
		<td><?=$item->draw_id ?></td>
		<td><?=($item['big_winner'] == 1) ? 'yes' : 'no' ?></td>
		<td><?=($item['published'] == 1) ? 'yes' : 'no' ?></td>
		<td><?=$this->Date($item['draw_date'],'medium')?></td>
		<td><?=$item['jackpot']; ?></td>
		<td>
		<a href="<?=$this->url(Array("action"=>"edit","id"=>$item->draw_id)) ?>">
			<?=$this->translate('edit')?>
		</a>
		</td>
	</tr>
	<? } ?>
</table>